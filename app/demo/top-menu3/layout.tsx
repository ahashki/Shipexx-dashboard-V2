import type React from "react"
import TopMenu3Layout from "@/components/demo/top-menu3-layout"

export default function DemoTopMenu3Layout({
  children,
}: {
  children: React.ReactNode
}) {
  return <TopMenu3Layout>{children}</TopMenu3Layout>
}
