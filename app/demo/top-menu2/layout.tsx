import type React from "react"
import TopMenu2Layout from "@/components/demo/top-menu2-layout"

export default function DemoTopMenu2Layout({
  children,
}: {
  children: React.ReactNode
}) {
  return <TopMenu2Layout>{children}</TopMenu2Layout>
}
